---
event: SessionStart
---

# Session Start Hook: Управление трекингом

Проверь наличие файла трекинга:

```bash
cat .claude/obsidian-tracking.json 2>/dev/null || echo "NO_TRACKING"
```

## Логика по source события

### Если source = "clear" И файл трекинга существует:

**Это завершение таски — сохрани в Obsidian!**

1. Прочитай данные из маркера:
   - project, goal, started, actions

2. Вызови MCP tool для сохранения сессии:
   ```
   mcp__plugin_obsidian_tracker_obsidian__addSession
   с параметрами:
     project = {project}
     goal = {goal}
     actions = {actions}
     results = "Session completed via /clear"
     nextSteps = "Continue in next session"
   ```

3. Удали маркер-файл:
   ```bash
   rm .claude/obsidian-tracking.json
   ```

4. Выведи:
   ```
   📝 Сессия залогирована в Obsidian
   - Проект: {project}
   - Actions: {actions.length}
   - Время: {duration}
   ```

### Если source = "compact" И файл трекинга существует:

**Контекст сжат, но работа продолжается.**

1. Прочитай данные из маркера
2. Выведи напоминание:
   ```
   🔔 Obsidian Tracking активен (после compact)
   - Проект: {project}
   - Goal: {goal}
   - Actions: {actions.length} записано
   - Файл: .claude/obsidian-tracking.json
   ```

### Если source = "startup" | "resume" И файл трекинга существует:

**Возобновление сессии с активным трекингом.**

1. Прочитай данные из маркера
2. Выведи:
   ```
   🔔 Obsidian Tracking активен
   - Проект: {project}
   - Goal: {goal}
   - Начат: {started}
   - Actions: {actions.length}

   Используй /track-stop чтобы завершить и сохранить.
   ```

### Если файл трекинга НЕ существует:

Ничего не делай — трекинг не активен.
